<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('ocr_product_ingredients', function (Blueprint $table) {
            $table->id();
            $table->foreignId('ocr_product_id')->constrained()->onDelete('cascade');
            $table->foreignId('ingredient_id')->constrained()->onDelete('cascade');
            $table->enum('status', ['halal', 'haram', 'syubhat', 'unknown']);
            $table->enum('risk_level', ['low', 'medium', 'high'])->default('medium');
            $table->string('source')->default('ocr');
            $table->timestamps();

            $table->unique(['ocr_product_id', 'ingredient_id']);
            $table->index(['ocr_product_id', 'status']);
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('ocr_product_ingredients');
    }
};
