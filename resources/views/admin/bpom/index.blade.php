@extends('admin.layouts.admin_layout')

@section('title', 'Manajemen Data BPOM')

@section('content')
<div class="container-fluid">
    <div class="row">
        <!-- Statistik -->
        <div class="col-md-3">
            <div class="card bg-primary text-white mb-4">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <div class="text-white-50 small">Total Produk</div>
                            <div class="text-lg font-weight-bold">{{ $stats['total'] }}</div>
                        </div>
                        <i class="fas fa-barcode fa-2x"></i>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card bg-success text-white mb-4">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <div class="text-white-50 small">Terverifikasi</div>
                            <div class="text-lg font-weight-bold">{{ $stats['verified'] }}</div>
                        </div>
                        <i class="fas fa-check-circle fa-2x"></i>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card bg-info text-white mb-4">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <div class="text-white-50 small">Generated by AI</div>
                            <div class="text-lg font-weight-bold">{{ $stats['ai_generated'] }}</div>
                        </div>
                        <i class="fas fa-robot fa-2x"></i>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card bg-danger text-white mb-4">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <div class="text-white-50 small">Potensi Bahaya</div>
                            <div class="text-lg font-weight-bold">{{ $stats['dangerous'] }}</div>
                        </div>
                        <i class="fas fa-exclamation-triangle fa-2x"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Filter & Search -->
    <div class="card mb-4">
        <div class="card-header bg-white">
            <i class="fas fa-filter mr-1"></i> Filter & Pencarian
        </div>
        <div class="card-body">
            <form action="{{ route('admin.bpom.index') }}" method="GET" class="form-row">
                <div class="col-md-4 mb-2">
                    <input type="text" name="search" class="form-control" placeholder="Cari Nama Produk, Merk, No. Reg..." value="{{ request('search') }}">
                </div>
                <div class="col-md-3 mb-2">
                    <select name="kategori" class="form-control">
                        <option value="">Semua Kategori</option>
                        <option value="obat" {{ request('kategori') == 'obat' ? 'selected' : '' }}>Obat</option>
                        <option value="kosmetik" {{ request('kategori') == 'kosmetik' ? 'selected' : '' }}>Kosmetik</option>
                        <option value="pangan" {{ request('kategori') == 'pangan' ? 'selected' : '' }}>Pangan Olahan</option>
                        <option value="suplemen" {{ request('kategori') == 'suplemen' ? 'selected' : '' }}>Suplemen Kesehatan</option>
                    </select>
                </div>
                <div class="col-md-3 mb-2">
                    <select name="status" class="form-control">
                        <option value="">Semua Status</option>
                        <option value="aman" {{ request('status') == 'aman' ? 'selected' : '' }}>Aman</option>
                        <option value="waspada" {{ request('status') == 'waspada' ? 'selected' : '' }}>Waspada</option>
                        <option value="bahaya" {{ request('status') == 'bahaya' ? 'selected' : '' }}>Bahaya</option>
                    </select>
                </div>
                <div class="col-md-2">
                    <button type="submit" class="btn btn-primary btn-block">
                        <i class="fas fa-search"></i> Cari
                    </button>
                </div>
            </form>
        </div>
    </div>

    <!-- Data Table -->
    <div class="card">
        <div class="card-header bg-white d-flex justify-content-between align-items-center">
            <span><i class="fas fa-table mr-1"></i> Daftar Data BPOM</span>
        </div>
        <div class="card-body px-0">
            <div class="table-responsive">
                <table class="table table-hover mb-0">
                    <thead class="bg-light">
                        <tr>
                            <th class="pl-4">No. Registrasi</th>
                            <th>Nama Produk</th>
                            <th>Kategori</th>
                            <th>Merk</th>
                            <th>Status Keamanan</th>
                            <th>Sumber</th>
                            <th>Aksi</th>
                        </tr>
                    </thead>
                    <tbody>
                        @forelse($bpom_data as $data)
                        <tr>
                            <td class="pl-4"><strong>{{ $data->nomor_reg }}</strong></td>
                            <td>{{ $data->nama_produk }}</td>
                            <td><span class="badge badge-secondary">{{ strtoupper($data->kategori) }}</span></td>
                            <td>{{ $data->merk }}</td>
                            <td>
                                @if($data->status_keamanan == 'aman')
                                    <span class="badge badge-success">AMAN</span>
                                @elseif($data->status_keamanan == 'waspada')
                                    <span class="badge badge-warning">WASPADA</span>
                                @else
                                    <span class="badge badge-danger">BAHAYA</span>
                                @endif
                            </td>
                            <td>
                                @if($data->sumber_data == 'ai')
                                    <span class="badge badge-info"><i class="fas fa-robot mr-1"></i> AI</span>
                                @else
                                    <span class="badge badge-dark">SISTEM</span>
                                @endif
                            </td>
                            <td>
                                <div class="btn-group">
                                    <a href="{{ route('admin.bpom.show', $data->id) }}" class="btn btn-sm btn-outline-primary" title="Detail">
                                        <i class="fas fa-eye"></i>
                                    </a>
                                    @if($data->verification_status != 'verified')
                                    <form action="{{ route('admin.bpom.verify', $data->id) }}" method="POST" class="d-inline">
                                        @csrf
                                        <button class="btn btn-sm btn-outline-success" onclick="return confirm('Verifikasi produk ini?')" title="Verifikasi">
                                            <i class="fas fa-check"></i>
                                        </button>
                                    </form>
                                    @endif
                                    <form action="{{ route('admin.bpom.destroy', $data->id) }}" method="POST" class="d-inline">
                                        @csrf
                                        @method('DELETE')
                                        <button class="btn btn-sm btn-outline-danger" onclick="return confirm('Hapus data ini?')" title="Hapus">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                        @empty
                        <tr>
                            <td colspan="7" class="text-center py-4 text-muted">Data tidak ditemukan.</td>
                        </tr>
                        @endforelse
                    </tbody>
                </table>
            </div>
            <div class="p-4 d-flex justify-content-center">
                {{ $bpom_data->links() }}
            </div>
        </div>
    </div>
</div>
@endsection
